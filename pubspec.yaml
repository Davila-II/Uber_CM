name: uber_cm
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.7

# C'est cette partie là qu'il faut bien vérifier
flutter:
  uses-material-design: true

  assets:
    - assets/images/transport_urbain.jpg